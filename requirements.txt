# Список зависимостей по названиям — без жёсткой фиксации версий: конкретные версии и их
# совместимость друг с другом (особенно Django/DRF/Celery) не тестировались вместе, проверить
# и зафиксировать (pip freeze) на момент старта разработки. Обоснование выбора каждой
# библиотеки — в docs/TECH_STACK.md.

# Веб-фреймворк
django>=5.2,<5.3          # LTS, см. docs/TECH_STACK.md п.1
djangorestframework       # REST API для ЛК, см. docs/INTEGRATION_LK.md
gunicorn                  # WSGI-сервер для прод-деплоя

# База данных
psycopg[binary]           # драйвер PostgreSQL
dj-database-url           # чтение DATABASE_URL из .env

# Фоновые задачи и расписание
celery
redis
django-celery-beat        # периодические задачи, редактируемые в Django admin

# Парсинг источников
feedparser                 # RSS/Atom
trafilatura                 # извлечение текста статьи со страницы
requests
cssselect                   # CSS-селекторы поверх lxml для html-источников (Source.parser_config)

# Дедупликация
rapidfuzz

# LLM-редактор: GigaChat (тариф для физлица, уровень Pro — см. docs/TECH_STACK.md п.5а).
# Проверить перед установкой актуальное официальное название/версию пакета на момент старта
# разработки (не тестировалось) — на 2026 это официальный SDK gigachat с PyPI.
gigachat

# Email-модерация: подпись ссылок штатным django.core.signing, отдельная библиотека не нужна.
# Отправка писем — UniSender Go через django-anymail (см. docs/TECH_STACK.md п.5б, решено 07.09.2026).
django-anymail

# Telegram: публикация новостей (клиент Bot API) и модуль chatbot (Dispatcher/FSM)
aiogram                    # см. docs/TECH_STACK.md пп.5 и 9
aiohttp-socks               # нужен aiogram для работы через прокси (TELEGRAM_PROXY_URL) — прямой
                             # доступ к Telegram с реального сервера заблокирован на сетевом
                             # уровне выше хостинг-провайдера, см. news/publishing/telegram.py

# Файловое хранилище для документов knowledge_base в продакшене (опционально на старте,
# см. docs/TECH_STACK.md п.8 — для локальной разработки можно обойтись локальным диском)
# django-storages
# boto3

# Опционально (см. docs/DEPLOYMENT.md — мониторинг очереди)
# flower
